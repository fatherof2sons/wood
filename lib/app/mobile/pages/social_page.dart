import 'package:flutter/material.dart';

class SocialPageMobile extends StatelessWidget {
  const SocialPageMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Contact"),
      ),
    );
  }
}
